// Re-export everything from the categories folder for backward compatibility
export { categories, getCategoryBySlug, categoryGroups } from './categories/index';
export type { Category } from './types';